[package]
name = "just-bash"
version = "3.0.0"
edition = "2024"
description = "Rust core for the just-bash virtual shell"
license = "Apache-2.0"

[lib]
name = "just_bash"
path = "src/lib.rs"

[[bin]]
name = "just-bash-rs"
path = "src/main.rs"
