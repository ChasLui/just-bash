[package]
name = "just-bash"
version = "3.0.0"
edition.workspace = true
description = "Rust core for the just-bash virtual shell"
license.workspace = true

[lib]
name = "just_bash"
path = "src/lib.rs"

[[bin]]
name = "just-bash-rs"
path = "src/main.rs"
