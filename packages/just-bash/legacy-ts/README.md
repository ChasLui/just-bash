# Legacy TypeScript implementation (archived)

The TypeScript runtime under this directory is intentionally kept as a historical implementation only.

Current canonical runtime is `packages/just-bash/rust-core`.

Do not add new features or maintenance tasks to files in this directory.
